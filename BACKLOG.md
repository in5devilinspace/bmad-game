# WHISPER HEIST — Improvement Backlog

Queue for the autonomous `bmad-dev-loop` agent. Top unchecked item = next iteration. Each item is one iteration's worth of player-visible value at low risk. The loop marks items done and appends to `docs/loop-log.md`. Ordered by value/risk; the loop may reorder or split.

Derived from `docs/spec/spec-whisper-heist/` Should-tier + polish. Do not touch items until the walking-skeleton gate has passed in `src/index.html`.

## Queue

- [x] Verify all six thieves (Vex, Silk, Brick, Nix, Ash, Moth) are distinguishable by conduct within one raid; if any read identical, widen their weight vectors. *(Verified distinguishable — no identical/same-trajectory pairs across seeds & nights. Hardened by fixing a decision-thrash deadlock: Moth ping-ponged survive↔protect-bond, re-tripping a plate ~14×/raid and arming the alarm; added goal hysteresis so conduct is now legible. See loop-log 2026-07-06.)*
- [x] Full 10-token vocabulary present and each renders a distinct legible glyph (solid vs hollow). *(Audited the ledger renderer `drawGlyph` (src/index.html ~L1160): all 10 token cases exist with distinct shapes and no '?'/generic fallback is reachable for the vocabulary. Found 2 tokens that ignored the firsthand/secondhand `solid` flag because they were stroke-only — SAFE_PATH (dashed line) and SHORTCUT (open arrow) — so a firsthand (solid) and secondhand (hollow) instance rendered identically, breaking CAP-3. SAFE_PATH is minted firsthand AND arrives secondhand via gossip, so the ambiguity was live in the hover ledger. Fix (render-only): SAFE_PATH now draws a solid continuous line firsthand / dashed secondhand; SHORTCUT now draws a filled arrowhead firsthand / open arrowhead secondhand. Other 8 already honored fill-vs-stroke. game-verifier confirmed pixel-level distinction (SAFE_PATH 1095 vs 820 lit px; SHORTCUT 1077 vs 1538) and live ledger rendering firsthand SAFE_PATH solid, 0 console errors. See loop-log 2026-07-06.)*
- [x] Deliberation beat (pause → eye-flick → wobbling intent arrow → commit) visible before every committed thief move. *(Was partially wired: the sim already paused each thief ~300ms before committing (`th.delib`, `commitPlan`) and exposed sorted `th.options`; render drew a pause ring + eye-flick dots + wobbling arrow. But `syncBeats` hardcoded `winner:null`, so the arrow never snapped — it wobbled and vanished, no visible resolve — and the runner-up shown ignored the actual committed pick under commitment-inertia. Render-only fix (src/index.html): `syncBeats` now sets `winner` from `th.pending` (a=top-scored option, b=the committed pick when inertia keeps a different goal, else the runner-up); `drawBeat` eases the intent arrow onto the winner over the final third of the pause (tt≥0.66) and brightens/enlarges the winning eye-flick dot as it snaps. Now every committed move shows pause→eye-flick→wobble→snap-to-winner→move, and the snap tracks what actually commits. game-verifier (live game): winnerNull=0 across 1189 delib samples; committed-winner consistency 0 violations (inertia 'b' case fired 171×); pause-ring+arrow screenshot captured; 0 console errors. See loop-log 2026-07-06.)*
- [x] Three-state trust glow (glow/neutral/grey) applied to every rendered token; confirm no numerals leak on the play HUD. *(Audited all 8 token-render sites. Reference impl (hover/pinned ledger `trustState` L1187/L1200-1206) was already correct. Found ONE trust defect: the in-world belief-ink overlay (L1074-1085) rendered token confidence as a **continuous** alpha ramp (`0.10+0.22*conf` ink, `0.5+0.5*conf` glyph) — not three states. Fixed render-only: bucketed through the shared `trustState()` at 0.66/0.33 → three ink densities (0.30/0.19/0.10) + glyph alphas (1/0.8/0.5), grey belief tints to `#96a0af`, glow gets the same shadow-halo cue as the ledger ring. Now every confidence display in the game is EXACTLY three states. Other sites exempt-by-design: truth layer (ground truth, no confidence), overhead goal glyph (indicator), pinned lineage (one token's hop history), debrief slide (transfer/mutation anim — firsthand-solid→hollow-bud), whisper picker (type menu), FX bubbles (transient). NUMERAL AUDIT: exhaustively enumerated all 13 `fillText`/`strokeText` sites + a runtime `fillText`/`strokeText` wrapper across title/raid/debrief — `DIGIT_LEAKS=[]`; only letters (thief initials V/S/B/N/A/M), token-type labels, `*` mutation marks, and fixed prose are drawn; night = crescent moons (`drawNightMoons`, canvas arcs), Chronicle outcome = pictorial moons/pips/gem-fate. Zero numerals on the play HUD. game-verifier PASS (trustState→['glow','glow','neutral','neutral','grey']; drawGlyph glow 2210 vs neutral 655 vs grey 621 lit px; 0 console errors). See loop-log 2026-07-06.)*
- [x] Debrief token-slide animation paced into exactly three readable beats by the beat scheduler. *(Was a raw per-index trickle: each gossip slide started at `si*150ms` and traveled 650ms, so ~24 tokens smeared into a continuous ~4.2s blur with no rhythm — not the "three readable beats" the render-plan Debrief stage specifies. Fix (presentation-only, all edits outside `<script id="sim">`): slides now carry a `beat` field (0/1/2), partitioned into exactly three contiguous waves by arrival order (`per=ceil(N/3)`, `beat=min(2,floor(s/per))`); GOSSIP events arrive grouped by ring-adjacent pair so each third is a coherent "these neighbors trade now" wave. A slide launches when its beat is released at `beat * GAP` (GAP=1500ms, now exported from the juice beat scheduler — the same one-dramatic-event-per-1.5s cap, so the debrief cadence is sourced from the scheduler constant, not a duplicate magic number). Old `_start`/`si*150` removed. Whisper UI flow and debrief→next-night progression untouched; click still fast-forwards. game-verifier (real Chrome): 24 slides = 8/8/8 across beats [0,1,2], no `_start`; three non-overlapping temporal waves with onsets 2ms / 1567ms / 3116ms (gaps ~1.565s / ~1.549s ≈ GAP); 0 console errors; night 1→2→3 cycle stable. `node tests/run-tests.mjs` green (GOSSIP log event untouched — trust-tint left out by design, consistent with prior "transfer animation, not a resting trust surface" call). 1972→1976 lines (<2000). See loop-log 2026-07-06.)*
- [x] Chronicle: every line references a run-specific logged token/event; two runs produce visibly different Chronicles. *(Audited `chronicle()`/`drawChronicle` (src/index.html): found 4 CANNED lines whose TEXT was identical every run — the opener ("Six went in under the dark…"), the alarm line ("Then the bells woke…"), the gem-survived closer ("The star kept its socket…never knew our names"), and the split-loot fallback — each gated on a real event but citing no run-specific datum. Also a live bug: the opener's `firstActor()` grabbed the `PLAN_SET 'seed'` marker, printing "seed led all six in…". Fix (render-only, inside juice IIFE, sim/gossip untouched): every line now composed via `add(line, ref)` where `ref` is the exact run-specific anchor it cites (a logged thief name / place-name of a logged cell / memory-token loc); the 4 canned lines now cite the run's first real actor, the last plate-tripper who armed the bells, a GEM_AT-belief holder, and loot counts (as words). Blame line strengthened: traces the planted lie by shared originId and counts how many of six carried it to the last night. Added two ALWAYS-present run-variable closing lines from the FINAL memory streams (the farthest-travelled rumor + the gang's divergent top beliefs) so two runs read materially differently even though the identically-seeded night-1 events don't. Zero numerals (words + pictorial moons/pips/gem). Added `tests/chronicle.test.mjs` + `tests/extract-full.mjs` (loads the sim AND render/juice blocks into one VM) — drives two seeds to the Chronicle, plants seed-derived whispers, and asserts (a) every line's ref is a substring of its line AND is independently re-derivable from THIS run's log+memory (non-circular), zero digits, no canned sentence survives, blame line present; (b) the two Chronicles differ by ≥3 lines. Wired into `tests/run-tests.mjs`. 44-seed sweep: 0 throws, 0 unanchored, 0 refs-outside-vocab, 0 digit-leaks, 44/44 distinct chronicles. game-verifier (real Chromium): 0 console errors, parchment renders run-specific prose (5 moons/pips/whole-gem), zero numerals, linesDiffer=3. 1976→1997 lines (<2000). See loop-log 2026-07-06.)*
- [x] Juice pass: slow-mo + screenshake on plate trip, alarm shockwave rings, banknote confetti on loot/gem grab — all governed by the one-event-per-1.5s beat queue. *(Done 2026-07-06; edit completed by the loop agent but it HUNG in its own verify step, so the orchestrator stopped it, verified the finished edit, and committed. Render-only (juice IIFE; `<script id="sim">` byte-unchanged). Three fixes: (1) screenshake vector cached once per frame into `Q.cam` in `tick()` — `camera()` used to re-randomize on every call, detaching the fx rings/confetti/bubbles from the shaking vault; now scene + fx shake in lockstep. (2) gem grab now also triggers slow-mo (`Q.slowmo=SLOWMO_MS`) alongside shake+confetti. (3) gem "coda": on a `gemStolen` raid end the main loop holds the frozen scene ~1400ms (`GEM_CODA_MS`), still ticking fx, so the heist's confetti/slow-mo render before the Chronicle. game-verifier PASS: `camera()` returns the single cached `Q.cam` every frame (13/13 distinct vectors during shake); beat queue gaps [1502,1502,1517]ms all ≥ GAP(1500); gem fx inject slow-mo 530 + confetti 46 without throwing; full 5-night run to Chronicle, 0 exceptions. `node tests/run-tests.mjs` green. 1997→2003 lines. See loop-log 2026-07-06.)*
- [x] **HIGH — Core stakes: the gem is effectively unstealable, so the player never has anything real to defend.** game-verifier's 300-seed × 5-night sweep (1500 autonomous raids) logged **GEM_GRAB: 0** (vs PLATE_TRIP 18,517, LOOT_GRAB 583). *(Fixed 2026-07-06. Root cause was a THREE-part depth-gating deadlock, found by sim probes, not guessed: (1) no belief ever pointed at the true gem cell (11,0) — `perceive` is circular behind the locked doors, and the only `GEM_AT` source, the `LOOT_AT→GEM_AT` type-slip, keeps the loot cell, so all "gem beliefs" were phantoms at the outer storerooms; D3/D4 unlocked 0/300 and no thief ever passed the y9 plate row. (2) Even given a gem belief, the `reach-gem` option carries the corridor's fear penalty, and by nights 4–5 exaggerated region/wing danger rumors (`PLATE_AT@hall-b`, `TRAP_AT@central`) poison the ENTIRE hall-b corridor (the only route to the sanctum), so `reach-gem` scored ~-4 and never won. (3) `beginNight` never reset `_planned`/`goalStack`, so the lockpick entered each night carrying the prior night's `survive` goal, and commitment inertia (0.25) kept it because the danger-immune follow-plan beat survive by only 0.03. Fix (sim/design): a maturing **"legend of the star"** GEM_AT(11,0) seeded into the gang for late-night planning (`emitRoutePlans`, night≥3) so Moth's DANGER-IMMUNE ROUTE_PLAN commits Silk to the sanctum on some seeds; **flight is now firsthand-only** — thieves flee/abort only from lethal danger they SAW (traps/armed-alarms), never from rumors or plates (plates are loud not lethal, per the hazard table), so a committed heister can push the plate row while rumors still shape cautious routing; **fresh plan each night** (reset `_planned`/`goalStack`); and a **whisper override** in `emitRoutePlans` so a planted GEM_AT/LOOT_AT decisively lures the gang onto or off the gem. Result: GEM_GRAB fires on ~13% of runs (shipped node sweep 6/48; test 5/24), all on nights 4–5, NEVER night 1 — a challenge, not a giveaway. Determinism + policy-equivalence + night-1 outer-loot guarantee all still pass; added `tests/gem-steal.test.mjs` (stealable-late / safe-early / not-all / determinism / whisper-lure both directions). See loop-log 2026-07-06.)*
- [x] Belief-ink overlay readability: a first-time viewer can spot a truth/belief divergence within 5 seconds; tune stroke alpha/color per thief. *(Done 2026-07-06. Root readability gap: `drawBeliefInk` rendered a TRUE belief and a PHANTOM belief identically — a planted TRAP_AT lie on a safe corridor drew the same faint ink + hollow star as a real, correctly-known trap, so a newcomer had to cross-reference the belief layer against the truth layer cell-by-cell (not a 5s spot), failing CAP-7. Fix (render-only, inside the render IIFE, `<script id="sim">` byte-unchanged): added a ground-truth point-feature index (`truthIndex`) + `isPhantom` predicate, and `drawBeliefInk` now flags any single-cell feature belief (TRAP_AT/PLATE_AT/ALARM_AT/LOOT_AT/GEM_AT/LOCKED_DOOR) whose cell has NO matching real feature as a divergence and draws a dashed broken "ghost ring" (`drawPhantomRing`) the truth layer never draws. Trust still drives the ring's strength (glow lie 0.62α / neutral 0.5 / grey 0.4 — three states preserved); per-thief provenance tint kept; zero numerals; closed vocabulary (no new glyph, just a halo). Region-loc rumors and non-feature tokens (SAFE_PATH/SHORTCUT/ROUTE_PLAN/VAULT_STYLE) never ring; the true "legend of the star" GEM_AT(11,0) correctly does NOT ring while phantom outer-storeroom gem beliefs do. Directly complements iteration 10: the planted-lie reroute's phantom trap is now the only thing on screen with a ghost ring. Exposed `truthIndex`/`isPhantom` on `WH.render`. Node probe (seed 0, real vault): real trap (3,5)=not-phantom, planted safe-cell lie=phantom, true gem legend=not-phantom, fake gem=phantom, real loot=not-phantom, multi-cell region rumor=not-phantom (all exact). game-verifier (real Chromium): 0 console errors / 0 uncaught / 0 failed requests, 6 thieves moving 5/6, flow to night 2; live-game probe confirmed isPhantom exact; screenshot + pixel probe — ghost ring lights only around the phantom (28/48 ring samples lit vs 12/48 around a real trap), belief-ink present at planted cell (1681/3136 px) vs empty control (29/3136). `node tests/run-tests.mjs` green (sim 31, gossip 35, chronicle 21, gem-steal 15, outsmart 15). 2053→2080 lines (<2400). See loop-log 2026-07-06.)*
- [x] Time-to-first-outsmart lands by night two: instrument a scripted run to confirm plan-lines reroute around a planted-safe hallway. *(Already worked — added the proof. The reroute mechanic (`DANGER_SURCHARGE=8` folds any believed hazard into A* every tick, since iteration 9) fires from night 1 onward, not just late nights, so the SPEC Success signal / CAP-6 lands by night 2. Added `tests/outsmart.test.mjs` (extractor-based, wired into run-tests.mjs): mirrors the integrator debrief lifecycle, plants a TRAP_AT lie on a SAFE hall-a corridor cell at the NIGHT-1 debrief, and asserts the lied-to thief's night-2 committed plan-line (`[pos, ...path]`, exactly what drawPlanLines draws) routes AROUND the cell while the same-seed control routes THROUGH it, to the SAME destination — plus CAP-4 lineage (target still holds TRAP_AT@C sourced to WHISPER), determinism, and night-1 win intact. Sweep seeds 0..15: 16/16 control-through, 16/16 rerouted, 16/16 same-destination; a second hall-b cell reroutes too (not a hard-coded fluke). Headline: seed 0, Vex, C=(8,9) → control up hall-b then west along y9 through (8,9); treatment detours the whole western stretch up to y8 (a full-row bend, very visible) to the same loot (2,12). No sim change needed; src/index.html byte-unchanged (test-only proof). game-verifier (real Chromium) PASS: 0 console errors, raid+6 thieves moving, flow to night 2; live in-page probe has(ctrl)=true / has(treat)=false / same destination (2,12); night-2 dashed-plan-line screenshot captured. Player-visibility already present: the planted lie renders as a glowing phantom-trap glyph (belief-ink, conf 0.9 → glow+halo) with the dashed plan-line bending around it against a truth layer showing no real trap. `node tests/run-tests.mjs` green (sim 31, gossip 35, chronicle 21, gem-steal 15 — iteration-9 balance intact, outsmart 15). See loop-log 2026-07-06.)*
- [x] Accessibility: keyboard-operable whisper UI, `prefers-reduced-motion` honored (no screenshake/parallax), focus-visible on all controls. *(Done 2026-07-06. Audit found all three gaps real: (a) keydown during the whisper phase only ever did "hold your tongue" — no keyboard way to select a thief / pick a token / place a cell; mouse-only. (b) No `matchMedia` anywhere — screenshake (`Q.shake`→`camera()`) and slow-mo (`Q.slowmo`→`timeScale()`) always fired. (c) Canvas had no `tabindex`, no `:focus-visible`, no on-canvas keyboard cursor. Fix (render/integrator only; `<script id="sim">` byte-IDENTICAL, verified): FULL keyboard path for the whisper flow — Arrow keys / Tab move an on-canvas selection cursor among the six thieves → seven rumor types (radial) → mini-map floor cells (arrows skip walls to the next floor cell), Enter/Space commits each step, Esc/Backspace "holds your tongue". Mouse and keyboard share the same transition helpers (`pickThief`/`pickType`/`placeLie`) so they can't drift; the mouse cursor seeds the keyboard cursor and vice-versa. Visible focus cue at every step: dashed teal ring on the focused seat, the picker's existing hover-highlight driven by keyboard for the type ring, a teal focus box on the mini-map cell; canvas got `tabindex=0` + a `:focus-visible` teal outline and auto-focus so keys work immediately. `prefers-reduced-motion` honored live via `matchMedia('(prefers-reduced-motion: reduce)')`: `activate()` skips shake/slow-mo (plate/alarm keep their rings, trap falls back to a ring so the shake-only beat isn't silent, gem keeps its confetti), and `camera()`/`timeScale()` are gated at the read chokepoint so even an in-flight shake is pinned to `{0,0}` the instant the OS pref flips — no reload. There is no true parallax layer to gate (confirmed). Keyboard help is discoverable: the three debrief hints now name arrows/enter/esc (zero numerals), and title/chronicle advertise "press a key". Added a read-only `WH._debug` handle (never wired to input, no gameplay effect) so the browser verifier could drive the flow without idling a full night. VERIFY: `node tests/run-tests.mjs` all green (sim 31, gossip 35, chronicle 21, gem-steal 15, outsmart 15); `node --check` (vm.Script) on all 3 inline blocks SYNTAX OK; headless node probe of the juice pipeline (mock matchMedia) 16/16 — motion-ON PLATE raises shake 13.3 + slow-mo + ring + `camera()` non-zero + `timeScale()` 0.35; motion-OFF suppresses shake/slow-mo but KEEPS the ring, trap falls back to a ring, gem keeps confetti, and a live pref-flip mid-shake pins `camera()` to {0,0}. game-verifier (real headless Chromium 1228, Playwright): PASS on all four gates — 0 console errors / 0 pageerrors, renders + reaches raid with 6 thieves moving; keyboard-ONLY whisper completed end-to-end (Brick, kType 0→2, kCell (11,13)→(11,11), whisperCount 0→1) with the on-canvas focus ring/box visible in screenshots; `Escape` skip left whisperCount unchanged; reduced-motion emulation live → `camera()` {0,0} + `timeScale()` 1 vs default `camera()` {-5.98,5.39} + 0.35; canvas `document.activeElement.id==='c'`. 2080→2190 lines (<2400). See loop-log 2026-07-06.)*
- [ ] Audio (optional, muted-by-default, single WebAudio oscillator kit): footstep ticks, alarm sting, confetti chime — no external assets.
- [x] Balance: confirm night-1 outer-loot win is guaranteed and the gem is genuinely unreachable before night 3. *(Confirmed empirically and LOCKED with a regression test — no src change needed. Broad node seed sweep via the shipped-block extractor: night-1 outer-loot win **300/300 (100%)** with the gem never stolen (0 timeouts, outcome exclusively 'outer-loot'); full 5-night autonomous runs steal the gem **29/300 (9.7%)**, ALL on nights 4-5 (`{4:24, 5:5}`), with **zero GEM_GRAB on nights 1-2** (indeed 0 on nights 1-3) — the sanctum is genuinely unreachable before night 3, consistent with the iteration-9 balance and the depth-gating table. Added `tests/balance.test.mjs` (wired into run-tests.mjs) that pins BOTH ends' opening guarantee so future tuning can't silently regress it: (A) 100-seed night-1 sweep asserts every raid ends 'outer-loot' with >=1 LOOT_GRAB and gemStolen=false and no night-1 GEM_GRAB; (B) 60-seed nights-1-2 sweep asserts zero GEM_GRAB events and gemStolen never set on nights 1-2; (B2) a mechanism lock on the "legend of the star" gate — the true-gem-cell GEM_AT is NOT seeded while planning nights <=3 and IS seeded once planning night 4 (game.night>=3). Complements tests/gem-steal.test.mjs which pins the other end (gem stealable on some nights 4-5, whisper lure both directions). Sweeps are shallow-by-design (night 1 / nights 1-2 only) so the lock stays broad but fast; dt=100 kept for fidelity with the other suites. `node tests/run-tests.mjs` all six suites green (balance 9/9); `src/index.html` byte-UNCHANGED. See loop-log 2026-07-06.)*
- [ ] Follow-up (from thief-distinguishability iteration): on nights 4–5 a thief (esp. Ash) can dither between two *same-goal* loot targets, re-crossing a plate ~4–5×. Goal hysteresis doesn't cover it (same goal, different target). Add lightweight target hysteresis / commit-to-nearest so the conduct reads as decisive greed, not indecision. Lower severity than the fixed Moth thrash; partly a probe artifact (harness omits Moth's ROUTE_PLAN options).
- [x] Test integrity: `build/sim.js` and `build/gossip.js` (what `tests/*.mjs` run against) have DRIFTED from the shipped `<script id="sim">` in `src/index.html` — e.g. build's `log()` lacks `ev.thief`/`ev.cell`, and build's `stepRaid` calls `activeState(game)` with no thief (no bond → no protect-bond option → it never reproduced the Moth thrash). Re-extract build artifacts from `src/index.html` (or add a `tests/run-tests.mjs` extractor per `interfaces.md`) so tests exercise shipped code. Confirm existing assertions still pass against the shipped block. *(Done 2026-07-06. Added `tests/extract-sim.mjs` — extracts the shipped `<script id="sim">` block from `src/index.html`, evals it in a Node VM, exposes `WH.sim`+`WH.gossip`; both `tests/*.mjs` now load through it. Added `tests/run-tests.mjs` (the entrypoint `interfaces.md` names) + `npm test`. All existing assertions pass unchanged against shipped code — no shipped bug found (`src/index.html` untouched). `build/*.js` frozen as historical scaffolding via `build/README.md` (single-source-of-truth = `src/index.html`), no re-mirroring, so drift can't recur. Proof the tests read shipped bytes: live `stepRaid`/`scoreOptions` source appears verbatim in `src/index.html`; shipped `stepRaid` lacks the bond-less `activeState(game)` drift marker that `build/sim.js` still has. See loop-log 2026-07-06.)*
